{
  "runcontract": 1,
  "services": [
    {
      "name": "frontend",
      "dir": "frontend",
      "stack": "web-vite",
      "kind": "server",
      "install": [["npm", "ci"]],
      "start": ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "${PORT}"],
      "port": 5173,
      "port_env": "PORT",
      "health": { "path": "/", "expect_status": [200] },
      "env": {
        "VITE_API_URL": { "dev": "http://localhost:8000" }
      }
    }
  ]
}
